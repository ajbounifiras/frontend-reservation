{
  "buildCommand": "npm install --legacy-peer-deps && ng build --configuration production",
  "outputDirectory": "dist/frontend-reservation",
  "framework": null,
  "build": {
    "env": {
      "NODE_VERSION": "16"
    }
  }
}
